# Solver Examples (`examples/`)

## Purpose
Contains clean, illustrative example programs demonstrating programmatic construction of models, invocation of the solver through the public C++ API, and retrieval of solution certificates.

## Execution Schedule
Example code will be introduced in tandem with the public C++ API implementation in `src/api/`.
