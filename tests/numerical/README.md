# Numerical Tests (`tests/numerical/`)

## Purpose
Houses specialized test suites for numerical conditioning, matrix scaling stability, pivot degeneracy handling, and floating-point edge cases (ill-conditioned bases, cycling prevention, extreme bounds).

## Execution Schedule
Numerical tests will be populated alongside the implementation of `src/numerics/` and algorithmic linear algebra routines.
