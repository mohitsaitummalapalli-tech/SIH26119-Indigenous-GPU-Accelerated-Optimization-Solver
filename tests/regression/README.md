# Regression Tests (`tests/regression/`)

## Purpose
Contains targeted test cases designed to prevent regressions for previously diagnosed bugs, numerical cycling incidents, or parser anomalies.

## Policy
Every reported and resolved bug must add a minimal reproducing instance to this directory before closure.
