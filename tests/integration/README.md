# Integration Tests (`tests/integration/`)

## Purpose
Houses end-to-end integration tests that verify full pipeline workflows: from parsing standard problem files (MPS/LP), formulating models, invoking algorithms, to validating solutions with the verification layer.

## Execution Schedule
Integration tests will be introduced during subsequent phases as IO, numerics, and algorithmic components are implemented.
